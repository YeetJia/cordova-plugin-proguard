android {
  // Deactivate R8
  buildTypes {
    debug {
      minifyEnabled false
    }
    release {
      minifyEnabled false
    }
  }
}

proguard {
  configurations {
    release {
      defaultConfiguration 'proguard-android-optimize.txt'
      configuration 'proguard-custom.txt'
    }
  }
}
